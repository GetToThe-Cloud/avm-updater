name: AVM Module Version Check

on:
  schedule:
    # Weekly on Mondays at 08:00 UTC
    - cron: '0 8 * * 1'
  workflow_dispatch:
    inputs:
      target_repository:
        description: 'Repository to scan (owner/repo). Leave empty to scan this repository.'
        required: false
        default: ''
      target_ref:
        description: 'Branch or ref to scan in the target repository. Leave empty for the default branch.'
        required: false
        default: ''
      include_risk:
        description: 'Risk tiers to include (comma-separated: LOW,MEDIUM,HIGH,UNKNOWN)'
        required: false
        default: 'LOW,MEDIUM,HIGH,UNKNOWN'
      dry_run:
        description: 'Dry run (no file changes, no PRs)'
        type: boolean
        required: false
        default: false

permissions:
  contents: write
  pull-requests: write

jobs:
  avm-update:
    name: Check and update AVM module versions
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Checks out a different repo when target_repository is specified.
      # Requires a PAT stored as GH_PAT with read/write access to the target repo.
      - name: Checkout target repository
        if: ${{ github.event.inputs.target_repository != '' }}
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.inputs.target_repository }}
          ref: ${{ github.event.inputs.target_ref }}
          token: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          path: target-repo

      - name: Resolve scan path
        id: config
        run: |
          if [ -n "${{ github.event.inputs.target_repository }}" ]; then
            echo "scan_path=./target-repo" >> "$GITHUB_OUTPUT"
          else
            echo "scan_path=." >> "$GITHUB_OUTPUT"
          fi

      - name: Install PowerShell 7
        uses: actions/setup-python@v5   # used to install pwsh on ubuntu
        continue-on-error: true

      - name: Ensure PowerShell 7 is available
        run: |
          if ! command -v pwsh &> /dev/null; then
            sudo apt-get update -qq
            sudo apt-get install -y powershell
          fi
          pwsh --version

      - name: Install Bicep CLI
        run: |
          curl -Lo bicep https://github.com/Azure/bicep/releases/latest/download/bicep-linux-x64
          chmod +x bicep
          sudo mv bicep /usr/local/bin/bicep
          bicep --version

      - name: Install Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1"

      - name: Install gh CLI
        run: |
          type gh &>/dev/null || (
            sudo apt-get update -qq &&
            sudo apt-get install -y gh
          )
          gh --version

      - name: Configure git identity
        run: |
          git config --global user.email "avm-updater[bot]@users.noreply.github.com"
          git config --global user.name  "avm-updater[bot]"

      - name: Build IncludeRisk array
        id: risk
        run: |
          RAW="${{ github.event.inputs.include_risk || 'LOW,MEDIUM,HIGH,UNKNOWN' }}"
          # Convert comma-separated to PowerShell array literal
          PS_ARRAY=$(echo "$RAW" | sed "s/,/','/g" | sed "s/^/'/" | sed "s/$/'/")
          echo "ps_array=@($PS_ARRAY)" >> "$GITHUB_OUTPUT"

      - name: Run Invoke-AvmUpdate
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
        run: |
          pwsh -NoProfile -Command "
            Set-StrictMode -Version Latest
            \$ErrorActionPreference = 'Stop'
            Import-Module ./AvmUpdater/AvmUpdater.psd1 -Force

            \$params = @{
              Path         = '${{ steps.config.outputs.scan_path }}'
              ApprovalMode = 'github'
              IncludeRisk  = ${{ steps.risk.outputs.ps_array }}
              OutputDirectory = './avm-report'
            }
            if ('${{ github.event.inputs.dry_run }}' -eq 'true') {
              \$params['DryRun'] = \$true
            }

            Invoke-AvmUpdate @params
          "

      - name: Upload report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: avm-update-report
          path: |
            avm-report/avm-update-report.md
            avm-report/avm-update-plan.json
          if-no-files-found: ignore
          retention-days: 30
